# Quant Trade System Master Update Plan V2

## 0. 文档目的

这份 V2 文档不再解决“系统骨架是否成立”的问题，而是面向下一阶段的三类优化：

1. `调优信号质量`
2. `积累训练样本`
3. `提升解释与交互效率`

当前系统的主框架已经基本成型：

- 新 WebUI 驾驶舱已落地
- 核心 ETF 引擎已落地
- 卫星仓雷达已落地
- 纪律层、Nightly、Robinhood CSV 回流、Change Feed、Slack / Email 已打通

V2 的目标是把系统从“功能完整”进一步推进到“更稳、更准、更值得长期依赖”。

---

## 1. V2 总体判断

### 1.1 当前最值得优化的不是“再加新功能”

下一阶段最值得做的，不是继续堆更多模型或页面，而是：

- 让已有信号更少噪声
- 让系统更清楚知道“哪些提醒真的有价值”
- 让解释更快、更便宜、更自然

一句话总结：

`V2 的主线是把系统从“会分析”升级到“更会筛噪声、更会复盘、更会解释”。`

### 1.2 优先级排序

建议按下面顺序推进：

1. `信号质量`
2. `训练样本沉淀`
3. `解释效率`
4. `局部产品打磨`

原因很简单：

- 没有更稳的信号，解释再漂亮也没有价值
- 没有样本沉淀，将来就训练不出真正有用的轻量模型
- 没有解释效率，LLM/SLM 会拖慢体验并增加成本

---

## 2. 调优信号质量

## 2.1 目标

V2 的第一目标不是让系统“更激进”，而是让它：

- 更少误报
- 更少无意义调仓
- 更早区分“强趋势”和“过热追涨”
- 更早识别“该停手”的环境

---

## 2.2 核心 ETF 信号质量优化

### 2.2.1 增加 `信号稳定性评分`

当前核心 ETF 已经有动作、目标权重和价位区间，但还可以再加一层稳定性评估：

- `signal_stability_score`
- `days_in_same_action`
- `days_since_regime_change`
- `weight_change_vs_threshold`

用途：

- 降低因为单日波动造成的误切换
- 帮助你判断“这个建议已经稳定几天了”

### 2.2.2 强化 `防抖 + 惯性`

当前已有：

- 最小权重调整阈值
- 动作确认天数

V2 可以继续增强：

- `连续 2-3 天同向支持才升级动作`
- `如果动作刚切换，短期内不允许再次反向切换`
- `在 HIGH 波动环境下自动提高阈值`

目标：

`减少 ETF 引擎自我折腾。`

### 2.2.3 把 `建议价位失效率` 接入复盘

要记录：

- 夜间给出的 ETF 买入区间，第二天是否真的有成交机会
- 如果跳空直接失效，失效率有多高
- 哪类 ETF 建议更常失效

如果某类建议长期“给得出，但实际无法执行”，就需要调规则，而不是继续信它。

---

## 2.3 卫星仓雷达信号质量优化

### 2.3.1 增加 `Top 3 稳定性约束`

V2 不建议让 Top 3 每天大换血。

建议新增：

- `minimum_top3_residency_days`
- `promotion_confirmation_days`
- `demotion_confirmation_days`

目标：

- 新票不要因为一天暴涨就挤进 Top 3
- 老票不要因为一天回调就立刻掉出 Top 3

### 2.3.2 把 `过热` 和 `确认` 分开

当前状态机已经比较清楚，但 V2 可以更严格：

- `CONFIRMED`
- `OVERHEATED_CONFIRMED`
- `BROKEN`

这能解决一个常见问题：

- 股票很强
- 但已经不适合买

系统应该能表达：

`趋势对，但位置不对。`

### 2.3.3 基本面项改成 `可选增强分`

高质量分析师预期、指引、财报电话会数据并不稳定。

所以 V2 明确建议：

- 把 `基本面加速` 拆成：
  - `基础财务加速分`
  - `预期/指引增强分`
- 没数据不扣分
- 有数据时标明 freshness 和 source

这样能避免：

`因为缺少高质量外部数据而系统性低估股票。`

---

## 2.4 纪律层信号质量优化

### 2.4.1 让纪律层开始“证明自己”

纪律层不是只负责阻止动作，它还需要回答：

- 如果遵守纪律层，结果如何
- 如果忽略纪律层，结果如何

V2 建议继续扩展：

- `follow_vs_ignore_expectancy`
- `follow_vs_ignore_realized_pl`
- `defensive_override_penalty_rate`
- `discipline_directional_hit_rate`

### 2.4.2 把 `停手信号` 做成更强的系统级结论

未来可以把这些条件整合成更清楚的 stop-hand logic：

- 风险 gate = `RISK_OFF`
- 分析过期严重
- 月度纪律恶化
- 候选池强信号明显减少

最终输出：

- `HEAVY`
- `NORMAL`
- `LIGHT`
- `STOP`

其中 `STOP` 需要更明显地压制所有新建仓建议。

---

## 3. 训练样本沉淀

## 3.1 为什么现在就要做

如果将来想训练：

- 盘中告警价值模型
- 卫星仓候选质量模型
- Top 3 晋升/降级辅助模型

那现在就必须系统化收集：

- 当时看到了什么
- 系统做了什么判断
- 用户是否执行
- 后来结果如何

没有这些标签，将来只能重新从头积累数据。

---

## 3.2 盘中事件样本层

当前已经有：

- `intraday_event_journal.jsonl`
- 盘中事件分类
- 收盘 outcome 标注

V2 建议继续补：

- `next_day_close_return_pct`
- `was_followed_by_trade`
- `matched_trade_price`
- `matched_trade_shares`
- `plan_context_signature`
- `discipline_regime_at_trigger`
- `risk_regime_at_trigger`
- `event_regime_at_trigger`

目标：

以后可以训练：

`哪些盘中提醒真的值得打断用户。`

---

## 3.3 Nightly 决策样本层

建议新增独立快照或在已有 journal 里系统化保留：

- 核心 ETF 建议
- Top 3 推荐
- 候选池 Top 10
- 月度纪律状态
- Change Feed 高优先级项

并给每一晚生成唯一 `decision_signature`。

后面就能做：

- 某一版规则下的建议长期表现
- 某一类 change feed 是否真的带来更好结果
- 某些计划是否总是“理论可行、实际难执行”

---

## 3.4 用户执行反馈样本层

Robinhood CSV 让这件事成为可能。

V2 建议进一步标准化三段式样本：

1. `系统建议`
2. `用户是否执行`
3. `执行后的短中期结果`

未来模型训练的最有价值标签，很可能不是“涨跌本身”，而是：

- `建议是否被执行`
- `执行后是否优于忽略`

这会比单纯预测涨跌更贴近实际交易。

---

## 4. 解释效率优化

## 4.1 原则

V2 的解释系统要遵循：

- 默认快
- 默认便宜
- 默认可追溯
- 只有在需要时才调用更重的模型

---

## 4.2 本地 SLM 与远程 LLM 分工

建议正式固定为：

### 本地 SLM（如 `Qwen/Qwen3-0.6B`）

本地 SLM 的定位不是“轻量解释器”，而是：

`结构化原因引擎的自然语言转述器`

适合：

- 将结构化 reason codes 改写成更自然的中文/英文
- 压缩长 bullet points
- Change Feed 的简短自然语言重述
- 把已有的结构化判断改写成更顺畅的用户提示

不适合：

- 自己判断“为什么变了”
- 独立承担复杂解释
- 独立承担调研、比较、推理

特点：

- 低成本
- 低延迟
- 适合高频转述与润色

### 远程 LLM

适合：

- 复杂解释
- 多信号联合解释
- 调研与资料整合
- 跨 ETF / 候选池 / 风险层的综合说明
- 夜间大报告总结
- 多条新闻 / 事件整合
- 复杂策略解释

特点：

- 更强，但应低频使用
- 应承担“真正解释”和“真正调研”的职责

---

## 4.3 结构化解释优先

V2 仍建议坚持：

- `原因来源` 必须结构化
- `语言润色` 才交给 SLM / LLM

不能让模型凭空猜“为什么信号变化了”，否则很容易出现：

- 听起来合理
- 但并不是系统真实的决策依据

所以 Change Feed / Core ETF / Satellite Radar 的解释应分两层：

1. `reason_codes + before/after + explanation_bullets`
2. 本地 SLM 做自然语言转述
3. 远程 LLM 只在需要更复杂解释时介入

---

## 4.4 解释缓存策略

建议继续强化缓存：

- key = `entity + snapshot_signature + model + prompt_version`

缓存对象：

- Core ETF 单卡解释
- Top 3 推荐解释
- Change Feed 每日总结
- 夜报解释段落

目标：

- 同一份快照不重复花钱
- 白天页面不重复等待

---

## 4.5 UI 层面的解释效率

建议继续优化：

- Core ETF 卡片默认只显示 1-2 句简要解释
- 点按钮才展开 LLM / SLM 解释
- Change Feed 默认只显示高优先级摘要
- 风险页重点先说“为什么不能做”，再给细节

解释不是越多越好，而是：

`在需要时能马上回答“为什么”。`

---

## 5. 当前不建议硬做 / 暂时做不了的项

下面这些不是“永远不做”，而是 `当前阶段不适合硬上`。

## 5.1 高质量分析师预期 / 指引自动化全覆盖

原因：

- 免费稳定源很差
- 数据 freshness 不可靠
- 容易让基本面评分看起来很精致，实际很脏

V2 结论：

- 只做增强项
- 不把它当主评分地基

## 5.2 用轻量模型直接替代结构化 Change Feed 原因引擎

原因：

- 容易“说得对，但不是系统真因果”
- 难追溯

V2 结论：

- 模型只能润色
- 原因引擎仍然必须结构化

## 5.3 训练“盘中告警价值模型”立即上线

原因：

- 样本还刚开始积累
- 标签规模明显不足

V2 结论：

- 先积累日志和 outcome
- 至少几周到几个月后再评估训练

## 5.4 把 LLM 直接接入交易主决策链

原因：

- 不可控
- 不可复盘
- 不利于纪律层

V2 结论：

- 继续严格禁止

## 5.5 自动接入 Robinhood 实盘 API

原因：

- 你当前明确不打算自动交易
- 真实需求是 CSV 回流，不是自动下单

V2 结论：

- 不作为近期方向

## 5.6 过早扩大卫星池到几千只股票

原因：

- 单用户系统噪声远大于收益
- 夜间分析成本和解释负担都会明显上升

V2 结论：

- 继续坚持：
  - 扫描宇宙有限扩展
  - 候选池 `<=100`
  - Top 深挖 `10-20`
  - Top 推荐 `<=3`

## 5.7 让本地 SLM 承担复杂解释或综合推理

比如：

- 多新闻整合
- 跨层决策说明
- 夜间长报告复杂总结

原因：

- 小模型更适合轻任务
- 强行上复杂任务会拖慢体验且质量不稳

V2 结论：

- 结构化原因转述本地
- 复杂解释 / 调研任务远程

---

## 6. 建议的下一阶段执行顺序

### Phase V2-A：信号质量优先

1. 核心 ETF 信号稳定性评分
2. Top 3 稳定性约束
3. ETF 建议失效率跟踪
4. 纪律层 follow / ignore 指标继续结构化

### Phase V2-B：样本沉淀优先

1. 扩展 `intraday_event_journal`
2. 为 nightly 决策增加统一 `decision_signature`
3. 固化“建议 -> 执行 -> 结果”训练样本格式

### Phase V2-C：解释效率优先

1. Change Feed 结构化解释继续丰富
2. Top 3 候选池按需解释按钮
3. SLM / LLM 缓存命中统计
4. 夜间解释摘要去重

### Phase V2-D：产品打磨

1. Dashboard 继续压缩噪声
2. Risk 页面更像“阻断页”
3. Control Center 更像“配置中台”
4. 进一步减少低价值技术暴露

---

## 7. 一句话总结

V2 不是为了让系统“更花哨”，而是为了让它：

- `更少做错`
- `更会积累数据`
- `更容易解释清楚`

如果 V1 解决的是“系统能不能跑起来”，  
那么 V2 解决的就是：

`这个系统能不能长期越用越聪明，越用越值得信。`
