# Jetson-safe requirements.
#
# Install NVIDIA's JetPack-compatible PyTorch manually first, then use this
# file for the rest of the app dependencies. This file intentionally does not
# include torch or chronos-forecasting, because normal pip dependency
# resolution can replace the NVIDIA CUDA-enabled torch wheel.

# Core app
fastapi==0.115.6
uvicorn[standard]==0.34.0
pandas==2.2.3
# NVIDIA's Jetson PyTorch wheels currently require the NumPy 1.x ABI.
numpy==1.26.1
pyarrow==19.0.1
yfinance==1.3.0
slack-bolt>=1.20,<2

# Backtesting engines
backtrader==1.9.78.123

# Deep learning support packages, but not torch itself.
transformers==4.52.3
accelerate==1.14.0
einops==0.8.2
