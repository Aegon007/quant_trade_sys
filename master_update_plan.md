# 估值雷达总体设计与实施状态

## 产品目标

系统只回答一个问题：市场中是否出现了“价格明显错位、基本面尚未永久受损、合理价值足以覆盖不确定性”的研究机会。

它不是持仓管理器、券商同步器、短期价格预测器或自动交易系统。个人持仓不会改变同一证券的估值结论。

## 已完成架构

### 研究域

- 可配置研究范围：S&P 500、Nasdaq 100、手工关注股票与独立 ETF；指数成分自动映射行业和行业 ETF，供异常收益计算使用。
- 两阶段计算：低成本全市场异常下跌初筛，随后只对高排名候选做财报、新闻、LLM 和估值深分析。
- 行情按 Stooq → yfinance 回退，本地 Parquet 缓存一小时，批量任务使用受控并发。
- 财务数字优先 SEC Company Facts，并严格按 `filed` 日期构造 point-in-time 指标；同步缓存 EDGAR 申报原文并提取 Business、Risk Factors、MD&A、市场风险等关键章节，无覆盖时显式标记 yfinance fallback。
- 事件分析区分短期冲击、潜在催化剂和结构性风险，不把新闻情绪直接当交易信号。

### 估值域

- LLM 路由器只能从模型白名单选择公司类型和估值方法，并基于结构化财务数字、SEC原文章节和事件证据返回财报摘要、风险、可追溯证据与三情景假设。
- 确定性引擎独立完成所有估值计算、兼容辅助模型交叉估值、Monte Carlo 区间、安全边际、模型离散度与数据覆盖置信度。
- 股票支持 FCFF、成长型收入 DCF、剩余收益、标准化盈利、收入倍数、REIT、SOTP 与困境加权。
- ETF 使用独立的风险溢价、收益率久期或现货持有方法，不套公司 DCF。
- 评分门槛来自 `valuation_policy.json`；高市场风险会自动提高最低安全边际。

### 治理域

- 推荐分为强机会、可积累研究、观察、等待企稳、价值陷阱、基本面受损、数据不足与高估。
- 每日每个标的只记录一次最新推荐，避免手工重跑形成重复样本。
- 周末按 63/126/252/504 个交易日同时校准相对短期国债代理 SGOV 的超额收益与相对 SPY 的市场超额，并保留估值类型横截面分布。
- Change Feed 只把新机会和基本面风险升级列为高优先级；轻微分数变化不占据首页。
- Manifest、任务状态、数据健康和诊断包提供运行审计与故障恢复线索。

### 交互域

- React/FastAPI 完全前后端分离，前端只读快照。
- 五页信息架构：研究首页、超跌机会、公司估值、市场风险、系统管理。
- 系统管理集中所有任务、Slack、Email、LLM、SLM、研究范围、阈值和调度设置。
- 可变设置采用“提交模板、本机覆盖”模式，WebUI 写入的实际 JSON 不进入 Git，避免多机同步冲突。
- Slack 使用适合聊天窗口的中文叙述，不发送 Markdown 表格。
- 夜间摘要与周末校准均可通过 Slack 和 Email 发送。

## 已删除范围

- TCN、Chronos、Foundation、多周期神经模型、训练、验证和 promote 全部删除。
- 传统策略注册、均线/RSI策略、Backtrader、旧回测与 Monte Carlo 交易预测全部删除。
- 持仓、资金、仓位纪律、Robinhood CSV、交易流水、券商动作和相关 UI/API/Slack 命令全部删除。
- 核心 ETF 轮动、卫星趋势雷达、盘中战术交易、旧新闻情报、分析师共识和旧周末相关性系统全部删除。
- 旧兼容路径、旧配置迁移和旧页面全部删除，不保留双轨实现。

## 数据契约

### 机会快照

每行至少包含：标的、资产类型、当前价、错位指标、合理价值 P10/P50/P90、安全边际、估值模型、可信度、基本面质量/损伤、困境概率、事件暂时性、企稳分、机会分、推荐、原因码和证据时间。

### 估值快照

每行至少包含：公司类型、主/辅估值模型、三情景假设、场景价值、概率区间、离散度、路由来源、验证警告、财务来源、财报期间与风险。

### 风险与反馈

市场风险只由市场数据计算。历史校准只评价系统当时的结论，不使用事后挑选，不以用户是否交易为条件。

## 完成标准

- 新代码不得导入已删除的模型、投资组合、交易、策略或 Robinhood 包。
- 未运行研究时 UI 必须明确显示缺失快照，而不是展示旧系统数据。
- 页面切换不触发行情、SEC、新闻或 LLM 调用。
- 手动任务必须返回 accepted 状态并持续更新真实进度、完成或错误。
- 所有密钥只能存在于忽略 Git 的本地秘密文件或环境变量。
- Python 测试与前端生产构建必须同时通过。

## 后续只做质量迭代

架构迁移已经完成。下一阶段不再增加新的“模型类型”，而是根据真实校准数据改进：财务字段覆盖率、假设约束、行业基准、事件证据质量、估值误差分解和不同市场状态下的推荐门槛。任何新功能都必须证明能提高机会质量、可靠性或使用效率，否则不进入系统。
